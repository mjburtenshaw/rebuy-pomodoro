import { useCountdowns } from './useCountdowns.hooks';
import { useService } from './useService.hooks';
import { useSnackbar } from './useSnackbar.hooks';

export const hooks = { useCountdowns, useService, useSnackbar };
