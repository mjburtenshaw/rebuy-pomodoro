import { StoryObj } from '@storybook/react';
import { LinearProgress } from './LinearProgress.styled';

type Story = StoryObj<typeof LinearProgress>;

export const LinearProgressMocks: {
  [key: string]: Story;
} = {
  default: {},
};
