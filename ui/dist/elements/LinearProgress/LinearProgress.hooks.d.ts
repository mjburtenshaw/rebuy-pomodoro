declare function useLinearProgress(initial: number, cap: number, increment: number, interval: number): {
    progress: number;
};
export declare const hooks: {
    useLinearProgress: typeof useLinearProgress;
};
export {};
